# Typst Best Practices

## Usage Constraint
- Follow a template-first workflow.
- Define reusable templates (header, body, styling, typography) before writing the actual content.

## Nim Interop
- Generate Typst content programmatically if dynamic documentation generation is needed.
